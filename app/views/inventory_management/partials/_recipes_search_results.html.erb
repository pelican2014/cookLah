<div id="recipes_search_results_info_and_table">

  <!-- info about search query -->
  <div>
    <%= page_entries_info @recipes %>
  </div>

  <!-- table of search results -->
  <table class="table">
    <tr>
      <th>Code</th>
      <th>Name</th>
      <th>Course and Region</th>
      <th>Symbol</th>
      <th>Dietary Restriction</th>
      <th>Cooking Time</th>
      <th>Level of Difficulty</th>
      <th>Price</th>
    </tr>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.code %></td>
        <td><%= recipe.name %></td>
        <td><%= recipe.course_and_region %></td>
        <td><%= recipe.symbol %></td>
        <td><%= recipe.dietary_restriction %></td>
        <td><%= recipe.cooking_time %></td>
        <td><%= recipe.level_of_difficulty %></td>
        <td><%= ingredient.price %></td>
      </tr>
    <% end %>
  </table>
</div>

<!-- page numbers -->
<%= paginate @recipes, :remote => true %>