// javascript response after ajax requests
// reload parts of view (or else ajax can disrupt overlay)
// reload strategic parts so that important information is updated

// reload recipes search results partial
$('#recipes_search_results').html('<%= escape_javascript(render '/inventory_management/partials/recipes_search_results') %>');

// reload ingredients search results partial
$('#ingredients_search_results').html('<%= escape_javascript(render '/inventory_management/partials/ingredients_search_results') %>');

// reload ingredients for recipe table
$('#ingredients_table_for_recipe').html('<%= escape_javascript(render '/inventory_management/partials/ingredients_table_for_recipe') %>');


// reload all the forms having the core path (the core path updates the ingredients chosen for recipe)
// reload add_ingredient_to_recipe_form_section
$('#add_ingredient_to_recipe_form_section').html('<%= escape_javascript(render '/inventory_management/partials/add_ingredient_to_recipe_form_section') %>');
// reload recipes search fields partial
$('#recipes_search_fields').html('<%= escape_javascript(render '/inventory_management/partials/recipes_search_fields') %>');
// reload ingredients search fields partial
$('#ingredients_search_fields').html('<%= escape_javascript(render '/inventory_management/partials/recipe_modal/ingredients_search_fields') %>');